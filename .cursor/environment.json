{
  "name": "Shifting Front",
  "install": "export PATH=\"$HOME/.nvm/versions/node/v22.22.2/bin:$PATH\" && yarn install --frozen-lockfile && yarn playwright install --with-deps chromium",
  "start": "export PATH=\"$HOME/.nvm/versions/node/v22.22.2/bin:$PATH\" && yarn dev --hostname 0.0.0.0 --port 3000",
  "ports": [3000]
}
